# HeatPump MCP Server Configuration

# Optional: EIA API key for live electricity rate lookups
# Get a free key at: https://www.eia.gov/opendata/register.php
# Without this, the server uses bundled fallback rates (2024 state averages)
EIA_API_KEY=

# Optional: NREL API key for advanced weather data
# Currently not used, but reserved for future enhancements
NREL_API_KEY=

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Local caching (true/false)
# Enables caching of API responses to reduce external calls
ENABLE_LOCAL_CACHE=true
